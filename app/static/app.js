// Minimal vanilla-JS enhancements -- no build step, no framework.
// Populated further in the Sync-Status Panel task (Task 8).
document.addEventListener("DOMContentLoaded", () => {
  // Intentionally empty at this stage; Task 8 adds sync-status polling here.
});
